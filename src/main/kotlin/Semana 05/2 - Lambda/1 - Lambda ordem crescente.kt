/*
Como posso usar uma função lambda para classificar uma lista em ordem crescente em Kotlin?
 */

fun main() {
    val myList = listOf(5, 3, 1, 4, 2)
    val sortedList = myList.sorted()

    println(sortedList);
}
